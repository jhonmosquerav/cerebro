# Bitácora operativa

Registro humano-legible del día a día (ingestas, consultas, mantenimiento).
Distinto de `genome/events.jsonl`, que registra solo mutaciones del genoma.
Formato: una línea por operación, lo más reciente arriba.

<!-- Anota aquí tus operaciones conforme uses CEREBRO (ONBOARD, INGEST, QUERY, LINT, CONSOLIDATE, EVOLVE, AUDIT). -->

## 2026-07-26
- INFRA (repaso de ecosistemas comparables → 2 capacidades nuevas en `tools/`, genoma intacto): revisados **AgentCairn** (memoria markdown + DuckDB/BM25/vectores), **Bedrock** (grafo Zettelkasten en Obsidian), **claude-obsidian** (ingesta + retrieval híbrido + locking multi-escritor) y **librarian-mcp** (MCP en Rust: auto-wikilinks, algoritmos de grafo). **Descartado por identidad de producto**: índice DuckDB, BM25+vectores+RRF+reranking y binario Rust (CEREBRO es "sin RAG, sin vectores, sin servidores, 0 deps"); reporte automático de errores a GitHub issues (choca con [[gen-confidencialidad]]); *hot cache* (ya lo hace el hook `SessionStart`). **Implementado**: `tools/cerebro_core/graph.py` — (1) **señales estructurales** deterministas sin dependencias (componentes, grado, hubs vs `hub_umbral`, huérfanas, puntos de articulación por Hopcroft-Tarjan iterativo, camino más corto) vía `python tools/cerebro.py graph [--scope wiki|genome|all]`, que cubre el hueco de que **tres genes activos declaraban consumir señales de hub/isla que el camino por defecto no producía** sin graphify instalado; (2) **LNK-03 enlace sugerido**, el detector *preventivo* que faltaba junto a LNK-01/LNK-02 — menciones en prosa de páginas existentes sin enlazar, `info`, con dos invariantes duros (no nombra destinos `confidencial`, no toma orígenes en cuarentena `riesgo_inyeccion`) y **sin auto-reescribir nunca** (a diferencia de librarian-mcp: aquí se PROPONE). **29 tests nuevos (144 → 173)**, `verify` en verde. Hallazgo de la primera corrida: `index.md` es el **único punto de articulación** del grafo completo (31 nodos / 111 aristas) — confirma el principio 3 y mide su fragilidad; y 5 positivos verdaderos de LNK-03 en `2026-07-02-86919843`. Integración a los genes **bajo compuerta**: `docs/propuestas-evolve/prop-f0-06` (gen-lint v6) y `prop-f0-07` (gen-graph-lens v4 + gen-jerarquizacion-indice v3), ambas `pending`. Solo se editó la lista de infra de `CLAUDE.md` (+ `mirror --fix`); la tabla de operaciones la mueven las propuestas.
- CHECKPOINT: `2026-07-26-30246132` — 0 notas a `working/` (nada valioso sin persistir; la decisión vive en el episódico y en esta bitácora), episódico creado, 0 candidatos a EVOLVE. Anclas sin cambio: las episódicas no se anclan ([[gen-jerarquizacion-indice]]).
- GIT (verificación de sincronía): `HEAD` ≡ `origin/main` ≡ `ls-remote refs/heads/main` = `f6d3fbc` · árbol limpio, sin untracked, stash vacío · rama local `fase-c-enforcement-mecanico` (`b701b0f`) ya fusionada en `main` (0 por delante), residual.
- LINT (reparación puntual, decisión de conservar): el aviso **LNK-02** sobre `wiki/episodic/2026-07-02-f1fc904c.md` se cierra **declarando la relación real**, no borrando la página — `2026-07-02-86919843` gana `relations: { sucede_a: "[[2026-07-02-f1fc904c]]" }` (verbo declarado, [[gen-entidad-con-estado]] vía gen-frontmatter-obligatorio v6). Se evaluó sacar ambas episódicas por ser "datos de uso" en el template y se **descartó**: son el dogfooding de la construcción de CEREBRO —no conocimiento de una empresa— y son la única evidencia viva que cierra el hallazgo **sev-4** de `2026-07-01-810f24e` ("el loop de memoria es aspiracional: `wiki/working/` y `wiki/episodic/` solo tienen `.gitkeep`"); además `audit/runs/2026-07-02-51690cb/10-maker.md:159` cita `86919843:59-60` **por línea** y `00-snapshot.md:36` inventaría las 2 páginas — borrarlas rompería citas de una corrida cerrada y claveada a SHA. El vacío de datos reales sigue garantizado por diseño de producto (README: el ONBOARD real va en un clon) y porque las episódicas **nunca se anclan** en `index.md` ([[gen-jerarquizacion-indice]]). Salud **87 → 100/100** (conectividad 100, 0 huérfanas); `verify` en verde sin avisos. Genoma intacto: sin mutación, sin línea en `events.jsonl`.

## 2026-07-12
- INFRA (Fase C del backlog `2026-07-01-810f24e` — los "validadores diferidos" — ejecutada como Fase 0 del roadmap de endurecimiento, `docs/roadmap-endurecimiento.md`): **núcleo mecánico en `tools/`** (Python stdlib, 0 deps) — C-01 lint de 16 detectores (frontmatter, wiki-links, verbos/campos fuera de esquema, vencidos duro/blando, identidad de página, ledger vs raw/, confidencial anclada) · C-02 espejo `AGENTS.md`≡`CLAUDE.md` · C-03 integridad del ledger (esquema + **hash-chain** en líneas nuevas + **append-only verificado contra los 90 commits reales**) · C-05 pre-commit (probado en 5 casos) · C-06 runbook de replay/rollback **ensayado de verdad** (`ops/runbook-replay.md`: el revert ingenuo fue detectado por EVT-07; el camino correcto quedó en verde). **`ONBOARD` aplicado ahora es un programa**: mismo manifiesto + misma fecha ⇒ mismo hash, idempotente — probado en CI (ubuntu + windows) y byte a byte en `worked/` (agencia-demo, legal-demo, con reviews honestos). `XRAY` (deriva declarado↔evidencia, graphify opcional) y `health` (87/100 hoy; la episódica `f1fc904c` huérfana baja conectividad) disponibles. 144 tests. **Genoma intacto**: la integración a los genes espera compuerta en `docs/propuestas-evolve/` (5 propuestas F0-01…F0-05, `status: pending`). README re-alineado a lo probado; `AGENTS.md` re-sincronizado.

## 2026-07-02
- APPLY (AUDIT 51690cb, segunda tanda — el operador aprobó C5–C16 completa; **16/16 hallazgos de la corrida cerrados**): **C5** gen-checkpoint v1→v2 + `stop.sh`/README exigen el frontmatter episódico completo y línea de log · **C6** gen-graph-lens v2→v3 (allowlist fail-closed + cuarentena `riesgo_inyeccion` fuera del staging, filtro actualizado en bash/PowerShell + checklist; cierra el pendiente A-07) · **C7** gen-query v4→v5 (log del fallback seudonimizado para confidenciales) · **C8** gen-query v5→v6 (cuarentena en el set "Advierte siempre") · **C12** gen-ciclo-de-vida v3→v4 (delta para tipos de fuente custom) · **C16** gen-ciclo-de-vida v4→v5 (retiro de ancla cubre hubs) — 6 mutaciones = 6 eventos + 6 commits. Fixes directos: C9/C13 (taxonomías + aplicado de onboard), C10 (README: fila CHECKPOINT + `archive/`), C11 (fila LINT completa en `CLAUDE.md`), C14/C15 (hooks: límite honesto de permissions + referencia de versión viva). `AGENTS.md` re-sincronizado. Ver `genome/events.jsonl` (63 líneas) y `audit/runs/2026-07-02-51690cb/30-proposals.md`.
- APPLY (AUDIT 51690cb, gate resuelto por el operador — corte ampliado a 4): **P1** verbos del genoma base reconciliados con el esquema de `relations` (gen-frontmatter-obligatorio v6 + gen-lint v4 + gen-consolidate v5 + gen-auto-auditoria v4; `supersede` unificado con `reemplaza`) · **P2** la cuarentena `riesgo_inyeccion` entra a promoción y fusión (gen-ciclo-de-vida v2 + gen-consolidate v6) · **P3** excepción declarada para el puntero rotatorio de CHECKPOINT (gen-jerarquizacion-indice v2) · **C4** transición de clave al mover de tier (gen-identidad-de-pagina v2 + gen-ciclo-de-vida v3). 9 mutaciones = 9 eventos + 9 commits; `AGENTS.md` re-sincronizado. **C5–C16 confirmados sin aplicar** → tanda EVOLVE/fixes de la próxima sesión. Ver `genome/events.jsonl` y `audit/runs/2026-07-02-51690cb/30-proposals.md`.
- AUDIT: corrida `2026-07-02-51690cb` (cierre de Fase A, sobre el genoma mutado) — maker de 4 especialistas + auditor independiente (barrera en disco). 16 candidatos → **13 confirmados, 3 ajustados, 0 refutados**. Patrón dominante: costuras entre propuestas EVOLVE redactadas en paralelo (4 contradicciones sev-5 entre genes de la tanda). Top-3 `status: pending` (gate humano): P1 verbos del genoma base fuera del esquema de `relations` (imp. 57), P2 cuarentena `riesgo_inyeccion` ausente de promoción/fusión (imp. 53), P3 CHECKPOINT ancla working/episodic contra el anclado determinista (imp. 52). C4 (id_pagina vs mover de tier, imp. 52) quedó fuera del corte solo por desempate. Ver `audit/runs/2026-07-02-51690cb/`.
- MIGRACIÓN ([[gen-migracion-genoma]], pase post-tanda): 25 genes en disco ≡ 25 en el índice de `CLAUDE.md`; `AGENTS.md` ≡ `CLAUDE.md` byte a byte; `events.jsonl` 48 líneas, todas parsean, append-only intacto (0 líneas previas tocadas); manifiesto de ejemplo al día (`identity`, `ciclo_de_vida`, `hub_umbral`). **1 hallazgo aplicado**: el episódico `2026-07-02-f1fc904c` declaraba `decay_rate: medium` siendo `clase: evento` → corregido a `high` (regla de [[gen-ciclo-de-vida]]).
- EVOLVE (C14/A-11): gen-confidencialidad v2→v3 + gen-query v3→v4 — la referencia indirecta a páginas `confidencial` tampoco expone metadatos reidentificadores (título, archivo, tags, relaciones); regla de nombrado seudónimo. **Cierra C14 de la corrida `2026-06-30-7c840d0`.** Ver `genome/events.jsonl`.
- EVOLVE (A-08): gen-raw-inmutable v1→v2 — excepción única y auditada de purga por incidente (4 condiciones conjuntivas: incidente en log + OK previo + línea `incident_purge` + re-AUDIT); `ops/runbook-git-seguro.md` §3.4 pasa a "vigente". Ver `genome/events.jsonl`.
- EVOLVE (A-06): gen-jerarquizacion-indice v1 (nuevo) + gen-ingest v2→v3 + gen-query v2→v3 + gen-consolidate v3→v4 + cap-ingesta v4→v5 — anclado determinista (muere el "si aplica"), partición en hubs al superar `hub_umbral` (7, manifiesto), fallback léxico sancionado de QUERY con transparencia y señal a LINT; principio 3 actualizado. Ver `genome/events.jsonl`.
- EVOLVE (A-05): gen-ciclo-de-vida v1 (nuevo) + gen-consolidate v2→v3 + gen-clase-temporal v1→v2 + gen-confianza-por-fuente v1→v2 — los números de la memoria por capas (decay 14/60/180d, refuerzo +0.10/+0.05/+0.03 con techo, promoción verificable, piso 0.30 → `wiki/archive/`); bloque `ciclo_de_vida` overridable en el manifiesto. Ver `genome/events.jsonl`.
- EVOLVE (A-04): gen-identidad-de-pagina v1 (nuevo) + gen-ingest v1→v2 + gen-bulk-ingest v1→v2 + cap-ingesta v3→v4 + gen-frontmatter-obligatorio v4→v5 — idempotencia por algoritmo: `id_pagina` determinista + `ingest-ledger.jsonl` append-only en la raíz (nace con el primer INGEST); campo `identity` en manifiesto y blueprint ecommerce. Ver `genome/events.jsonl`.
- EVOLVE (A-03): gen-checkpoint v1 (nuevo) + operación `CHECKPOINT` — el loop de memoria pasa a contrato multi-implementación (hooks automáticos + CHECKPOINT manual portable), idempotente por clave de sesión. Ver `genome/events.jsonl`.
- EVOLVE (A-01): gen-anti-inyeccion v1 (nuevo) + cap-ingesta-de-fuente v2→v3 — contenido de `raw/`/`wiki/` es dato, jamás instrucción; la clasificación nunca la delega el documento; cuarentena `riesgo_inyeccion` + PII-halt reforzado. **Cierra la sev-5 (OWASP LLM01) de la evaluación.** Ver `genome/events.jsonl`.
- COMPUERTA (operador): tanda completa de 7 propuestas EVOLVE de `2026-07-01-810f24e` **aprobada** ("Aprobar las 7 en orden") tras presentación en gate; 20 mutaciones aplicadas (5 genes nuevos: 20→25), 1 commit por mutación, orden del backlog A-01→A-03→A-04→A-05→A-06→A-08→C14 con versiones encadenadas. Fase A previa commiteada aparte (`6596596`).
- EJECUCIÓN Fase A (9 agentes en paralelo, dominios de archivos disjuntos): **A-02** hooks reales v1 (`.claude/hooks/*.sh`, 12 casos probados; activos al reiniciar sesión) · **A-10** `permissions` (deny `raw/**`, ask `genome/**`) · **A-07** staging de la lente a allowlist fail-closed (doble cerrojo, probado con fixtures) · **A-08/A-09** `ops/` (git seguro + backup cifrado con `--verify-restore` probado) · **A-11** README alineado a evidencia + C9 cerrado en dashboards. **7 propuestas EVOLVE `pending`** en `audit/evaluations/2026-07-01-810f24e/70-propuestas-evolve/` (a01 anti-inyección, a03 CHECKPOINT, a04 identidad+ledger, a05 umbrales, a06 índice+QUERY, a08 excepción purga, c14 metadatos) — **genoma intacto**, esperando compuerta. `CLAUDE.md`≡`AGENTS.md` re-sincronizados (nota de hooks: stubs → v1; SHA-256 verificado).
- BACKLOG (decisión del operador sobre el plan 50): reordenado a **técnico → validación → validadores diferidos → mercado**, sin tiempo, prioridad por orden. Fase A = versión mejorada operando (anti-inyección, hooks reales, CHECKPOINT, idempotencia por identidad de página, umbrales de ciclo de vida, jerarquización de índice, allowlist staging, git seguro, backup, README honesto); Fase B = piloto Fase 0; Fase C (validadores/hash-chain/firma) diferida con riesgo aceptado documentado. Ver `audit/evaluations/2026-07-01-810f24e/60-backlog.md`.
- PLAN (derivado de la evaluación `2026-07-01-810f24e`): escenario objetivo **Escritura Pública** elegido por deseabilidad (5/5) y fit con fortalezas verificadas (5/5), con advertencia explícita de que no es el más probable hoy (la señal actual favorece Todo Incluido). Plan F0–F4 (seguridad+honestidad → piloto Fase 0 ≤90 días → garantía mecánica "fe pública" → fabricar operadores → capturar categoría) + coberturas ≤15% + watchlist trimestral con tripwires. Ver `audit/evaluations/2026-07-01-810f24e/50-plan-escritura-publica.md`. Sin mutaciones de genoma (las propuestas del plan pasarán por EVOLVE/compuerta cuando se ejecuten).

## 2026-07-01
- EVALUACIÓN (ejercicio externo, no operación del genoma): `2026-07-01-810f24e` — panel multidisciplinar de 6 lentes de industria (ISO 25010+ATAM, APQC KM, COBIT+ISO 31000, 12-Factor Agents, STRIDE+OWASP LLM, TRL+SWOT) con auditoría cruzada maker≠auditor (119 afirmaciones re-derivadas: 104 confirmadas, 15 matizadas, 0 refutadas) + escenarios método Schwartz/GBN. Valoración global **2.8/5** (diseño/gobernanza 3.5–4.5; validación viva y enforcement 1.5–2), **TRL 4**. Artefactos en `audit/evaluations/2026-07-01-810f24e/` (sin commitear; genoma intacto).

## 2026-06-30
- APPLY (AUDIT C7): `index.md` actualizado (updated 2026-06-30) — anclas de operación `GRAPH`/[[gen-graph-lens]] y de la capa de visualización; el mapa de entrada refleja el genoma vigente.
- APPLY (AUDIT C8): runbook de grafo — "backend local" → "con el backend de `graph_lens.backend`" (coherente con gen-graph-lens v2).
- APPLY (AUDIT P5/C6): `dashboards/graph/00-leeme.md` — filtro de staging endurecido (patrón tolerante espacios/comillas, rutas preservadas en PowerShell, verificación bloqueante antes de graphify).
- APPLY (AUDIT P4/C5): cap-ingesta-de-fuente v1->v2 — el workflow de INGEST ahora compone `gen-confidencialidad` (clasifica sensibilidad + PII-halt; no ancla confidenciales). Ver `genome/events.jsonl`.
- APPLY (AUDIT P3): `default_sensibilidad: confidencial` en `salud.yaml` y `legal.yaml` — las páginas sensibles nacen confidencial (no se anclan/fusionan/exportan).
- APPLY (AUDIT P2): bloque `graph_lens` añadido a los 5 blueprints de onboard — ONBOARD v4 ya tiene dónde persistir `graph_lens.backend`.
- APPLY (AUDIT P1): gen-frontmatter-obligatorio v3->v4 — default de `sensibilidad` = `default_sensibilidad` del manifiesto (antes fijo `interno`, contradecía gen-confidencialidad). Ver `genome/events.jsonl`.
- AUDIT: corrida `2026-06-30-7c840d0` — equipo de 7 especialistas (maker) + auditor independiente (barrera maker≠auditor en disco). 15 candidatos → 13 confirmados, 1 refutado, 1 degradado. Top-3 `status: pending` (gate humano): P1 contradicción default `sensibilidad` entre genes (sev5), P2 blueprints sin bloque `graph_lens` (sev4), P3 blueprints sensibles sin `default_sensibilidad` (sev4). Ver `audit/runs/2026-06-30-7c840d0/`.
- EVOLVE: gen-onboard v3->v4 — al configurar, si la lente está activa sin backend, ONBOARD pregunta una vez y lo registra. Ver `genome/events.jsonl`.
- EVOLVE: gen-graph-lens v1->v2 — backend elegible por el usuario (claude|local|structural), registrado en el manifiesto; invariante = lo confidencial nunca sale. Ver `genome/events.jsonl`.
- EVOLVE: gen-graph-lens v1 (nuevo) + operación `GRAPH` — analítica de grafo como señales a CONSOLIDATE/QUERY/LINT/EVOLVE. Ver `genome/events.jsonl`.
- EVOLVE: gen-visualizacion v1->v2 — render interactivo (lente de grafo graphify, opcional/local). Ver `genome/events.jsonl`.

## YYYY-MM-DD
- _(tu primera operación: corre `ONBOARD` y regístrala aquí)_
